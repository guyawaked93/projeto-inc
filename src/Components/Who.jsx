import styled from "styled-components";

const Section = styled.div`
  height: 100vh;
  scroll-snap-align: center;
  color: white;
`;

const Who = () => {
    return(
        <Section>Who</Section>
    );
};

export default Who;